const initialState = {
    id: 0,
    name: "",
    description: "",
    salary: 0
};

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};